apiVersion: enclii.dev/v1
kind: Service
metadata:
  name: autoswarm-nexus-api
  project: madfam-platform
  description: AutoSwarm Office — Core orchestration API
spec:
  build:
    type: dockerfile
    dockerfile: infra/docker/Dockerfile.nexus-api
    context: .
    source:
      git:
        repository: https://github.com/madfam-org/autoswarm-office
        branch: main
        autoDeploy: true
  runtime:
    port: 4300
    replicas: 1
    resources:
      requests:
        cpu: "50m"
        memory: "512Mi"
      limits:
        cpu: "500m"
        memory: "1Gi"
    healthCheck: /api/v1/health
    readinessProbe:
      path: /api/v1/health
      initialDelaySeconds: 10
      periodSeconds: 5
    livenessProbe:
      path: /api/v1/health
      initialDelaySeconds: 30
      periodSeconds: 10
  env:
    - name: PORT
      value: "4300"
    - name: DATABASE_URL
      valueFrom:
        secretKeyRef:
          name: autoswarm-secrets
          key: database-url
    - name: REDIS_URL
      valueFrom:
        secretKeyRef:
          name: autoswarm-secrets
          key: redis-url
    - name: JANUA_ISSUER_URL
      value: https://auth.madfam.io
    - name: JANUA_CLIENT_ID
      value: autoswarm-office
    - name: JANUA_CLIENT_SECRET
      valueFrom:
        secretKeyRef:
          name: autoswarm-secrets
          key: janua-client-secret
  domains:
    - domain: agents-api.madfam.io
      tls: true
      tlsIssuer: cloudflare
      primary: true
  slo:
    availability: 99.9
    latencyP99Ms: 500
---
apiVersion: enclii.dev/v1
kind: Service
metadata:
  name: autoswarm-office-ui
  project: madfam-platform
  description: AutoSwarm Office — Spatial office UI
spec:
  build:
    type: dockerfile
    dockerfile: infra/docker/Dockerfile.office-ui
    context: .
    args:
      NEXT_PUBLIC_API_URL: https://agents-api.madfam.io
      NEXT_PUBLIC_JANUA_URL: https://auth.madfam.io
      NEXT_PUBLIC_COLYSEUS_URL: wss://agents-ws.madfam.io
    source:
      git:
        repository: https://github.com/madfam-org/autoswarm-office
        branch: main
        autoDeploy: true
  runtime:
    port: 3000
    replicas: 1
    resources:
      requests:
        cpu: "25m"
        memory: "256Mi"
      limits:
        cpu: "500m"
        memory: "512Mi"
    healthCheck: /api/health
    readinessProbe:
      path: /api/health
      initialDelaySeconds: 10
      periodSeconds: 5
    livenessProbe:
      path: /api/health
      initialDelaySeconds: 30
      periodSeconds: 10
  env:
    - name: NEXT_PUBLIC_DHANAM_URL
      value: https://billing.madfam.io
  domains:
    - domain: agents.madfam.io
      tls: true
      tlsIssuer: cloudflare
      primary: true
---
apiVersion: enclii.dev/v1
kind: Service
metadata:
  name: autoswarm-colyseus
  project: madfam-platform
  description: AutoSwarm Office — Colyseus room server
spec:
  build:
    type: dockerfile
    dockerfile: infra/docker/Dockerfile.colyseus
    context: .
    source:
      git:
        repository: https://github.com/madfam-org/autoswarm-office
        branch: main
        autoDeploy: true
  runtime:
    port: 4303
    replicas: 1
    resources:
      requests:
        cpu: "50m"
        memory: "256Mi"
      limits:
        cpu: "500m"
        memory: "512Mi"
    healthCheck: /health
    readinessProbe:
      path: /health
      initialDelaySeconds: 5
      periodSeconds: 5
    livenessProbe:
      path: /health
      initialDelaySeconds: 15
      periodSeconds: 10
  domains:
    - domain: agents-ws.madfam.io
      tls: true
      tlsIssuer: cloudflare
      primary: true
---
apiVersion: enclii.dev/v1
kind: Service
metadata:
  name: autoswarm-admin
  project: madfam-platform
  description: AutoSwarm Office — Admin dashboard
spec:
  build:
    type: dockerfile
    dockerfile: infra/docker/Dockerfile.admin
    context: .
    args:
      NEXT_PUBLIC_API_URL: https://agents-api.madfam.io
      NEXT_PUBLIC_JANUA_URL: https://auth.madfam.io
    source:
      git:
        repository: https://github.com/madfam-org/autoswarm-office
        branch: main
        autoDeploy: true
  runtime:
    port: 3000
    replicas: 1
    resources:
      requests:
        cpu: "25m"
        memory: "256Mi"
      limits:
        cpu: "500m"
        memory: "512Mi"
    healthCheck: /api/health
    readinessProbe:
      path: /api/health
      initialDelaySeconds: 10
      periodSeconds: 5
    livenessProbe:
      path: /api/health
      initialDelaySeconds: 30
      periodSeconds: 10
  domains:
    - domain: agents-admin.madfam.io
      tls: true
      tlsIssuer: cloudflare
      primary: true
---
apiVersion: enclii.dev/v1
kind: Service
metadata:
  name: autoswarm-gateway
  project: madfam-platform
  description: AutoSwarm Office — WebSocket gateway
spec:
  build:
    type: dockerfile
    dockerfile: infra/docker/Dockerfile.gateway
    context: .
    source:
      git:
        repository: https://github.com/madfam-org/autoswarm-office
        branch: main
        autoDeploy: true
  runtime:
    port: 4304
    replicas: 1
    resources:
      requests:
        cpu: "25m"
        memory: "256Mi"
      limits:
        cpu: "500m"
        memory: "512Mi"
    healthCheck: /health
    readinessProbe:
      path: /health
      initialDelaySeconds: 5
      periodSeconds: 5
    livenessProbe:
      path: /health
      initialDelaySeconds: 15
      periodSeconds: 10
  domains:
    - domain: agents-gw.madfam.io
      tls: true
      tlsIssuer: cloudflare
      primary: true
---
apiVersion: enclii.dev/v1
kind: Service
metadata:
  name: autoswarm-workers
  project: madfam-platform
  description: AutoSwarm Office — Background task workers
spec:
  build:
    type: dockerfile
    dockerfile: infra/docker/Dockerfile.workers
    context: .
    source:
      git:
        repository: https://github.com/madfam-org/autoswarm-office
        branch: main
        autoDeploy: true
  runtime:
    replicas: 1
    resources:
      requests:
        cpu: "100m"
        memory: "512Mi"
      limits:
        cpu: "1000m"
        memory: "1Gi"
  env:
    - name: DATABASE_URL
      valueFrom:
        secretKeyRef:
          name: autoswarm-secrets
          key: database-url
    - name: REDIS_URL
      valueFrom:
        secretKeyRef:
          name: autoswarm-secrets
          key: redis-url

apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: selva
  labels:
    app.kubernetes.io/name: gateway
    app.kubernetes.io/part-of: selva-office
    app.kubernetes.io/component: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: gateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gateway
        app.kubernetes.io/part-of: selva-office
        app.kubernetes.io/component: gateway
    spec:
      serviceAccountName: selva-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: gateway
          image: ghcr.io/madfam-org/selva-gateway@sha256:7815f1accd169e765bb4c663ff686aca9a4f73ce2584dd3d420a8a2773b4d06a
          ports:
            - containerPort: 4304
              protocol: TCP
          resources:
            requests:
              cpu: 25m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 4304
            initialDelaySeconds: 5
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 4304
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: selva-config
            - secretRef:
                name: selva-secrets
          env:
            - name: ENVIRONMENT
              value: production
            - name: AUTO_DISPATCH_ENABLED
              value: "true"
            - name: NEXUS_API_URL
              value: "http://nexus-api.selva.svc.cluster.local"
            - name: PHYND_CRM_URL
              value: "http://phynd-crm-web.phynd-crm.svc.cluster.local"
            - name: PHYND_CRM_TOKEN
              valueFrom:
                secretKeyRef:
                  name: selva-secrets
                  key: PHYND_CRM_TOKEN
            - name: RESEND_API_KEY
              valueFrom:
                secretKeyRef:
                  name: selva-secrets
                  key: RESEND_API_KEY
            - name: WORKER_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: selva-secrets
                  key: WORKER_API_TOKEN
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: data
              mountPath: /app/data
      volumes:
        - name: tmp
          emptyDir: {}
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: selva
  labels:
    app.kubernetes.io/name: gateway
    app.kubernetes.io/part-of: selva-office
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gateway
  ports:
    # Named so ServiceMonitor/selva-gateway (service-monitors.yaml, `port: http`)
    # resolves an endpoint: the operator matches endpoints to Service ports by
    # NAME only. Unnamed, the monitor matched zero targets.
    - name: http
      port: 80
      targetPort: 4304
      protocol: TCP

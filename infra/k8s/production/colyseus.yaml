apiVersion: apps/v1
kind: Deployment
metadata:
  name: colyseus
  namespace: autoswarm
  labels:
    app.kubernetes.io/name: colyseus
    app.kubernetes.io/part-of: autoswarm-office
    app.kubernetes.io/component: realtime
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: colyseus
  template:
    metadata:
      labels:
        app.kubernetes.io/name: colyseus
        app.kubernetes.io/part-of: autoswarm-office
        app.kubernetes.io/component: realtime
    spec:
      serviceAccountName: autoswarm-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: colyseus
          image: ghcr.io/madfam-org/autoswarm-colyseus:latest
          ports:
            - containerPort: 4303
              protocol: TCP
          resources:
            requests:
              cpu: 50m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 4303
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 4303
            initialDelaySeconds: 20
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: autoswarm-config
            - secretRef:
                name: autoswarm-secrets
          env:
            - name: ENVIRONMENT
              value: production
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: colyseus
  namespace: autoswarm
  labels:
    app.kubernetes.io/name: colyseus
    app.kubernetes.io/part-of: autoswarm-office
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: colyseus
  ports:
    - port: 80
      targetPort: 4303
      protocol: TCP

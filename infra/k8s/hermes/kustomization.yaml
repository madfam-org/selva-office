---
# Kustomize overlay — Hermes integration infra
# Apply with:  kubectl apply -k infra/k8s/hermes/
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: autoswarm

resources:
  - pvc-skills.yaml
  - secret-hermes.yaml

commonLabels:
  app.kubernetes.io/part-of: selva
  app.kubernetes.io/managed-by: kustomize
